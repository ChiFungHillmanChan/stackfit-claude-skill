# <System Name> — Build Spec

> Generated by system-design-architecture on <DATE>. Prices checked <DATE>.
> This is a build document. Decisions here are constraints, not suggestions.
> Rationale and rejected alternatives live in the companion HTML.

## Rules For The Implementing Agent

1. Do not substitute services. If a constraint here appears wrong, stop and raise it rather than choosing an alternative.
2. Do not add infrastructure not listed here. Every component below cites a requirement; anything absent was deliberately excluded.
3. Build in checklist order. Later steps assume earlier ones.
4. Fields marked `[assumed]` were inferred, not confirmed. Verify before relying on them for anything expensive or hard to reverse.

## Requirements

| Field | Value | Source |
|---|---|---|
| Write TPS | | |
| Read TPS | | |
| p99 latency | | |
| Availability | | |
| Consistency | | |
| Durability | | |
| Data volume yr 1 | | |
| Budget ceiling | | |
| Compliance | | |
| Team size | | |

## Stack

Exact versions and instance classes. Not families.

| Layer | Service | Version / class | Justified by |
|---|---|---|---|
| Compute | | | |
| Database | | | |
| Cache | | | |
| Queue | | | |
| CDN / edge | | | |

## Schema

Full DDL. Every index carries the query it serves. An index without a named query does not belong here.

```sql
CREATE TABLE example (
  id          BIGSERIAL PRIMARY KEY,
  user_id     BIGINT NOT NULL REFERENCES users(id),
  created_at  TIMESTAMPTZ NOT NULL DEFAULT now()
);

-- serves: feed query, "latest N posts for a user"
CREATE INDEX idx_example_user_created ON example (user_id, created_at DESC);
```

### Index justification

| Index | Serves query | Expected selectivity |
|---|---|---|
| | | |

## API Surface

| Method | Path | Auth | Expected p99 | Notes |
|---|---|---|---|---|
| | | | | |

## Caching

Every entry needs an invalidation rule. A cache without stated invalidation is not part of this design.

| Key pattern | Contents | TTL | Invalidated by |
|---|---|---|---|
| | | | |

Cold-start behavior: <what happens when the cache is empty; state the stampede guard>

## Environment Variables

| Name | Purpose | Secret |
|---|---|---|
| | | |

## Build Checklist

Ordered. Each item is a concrete action.

1.
2.
3.

## Scaling Triggers

| Tier | Move up when | Cost |
|---|---|---|
| MVP | | |
| Growth | | |
| Scale | | |

## Assumptions To Verify

Uncorrected inferences from the interview. Each states what changes if it turns out wrong.

| Assumption | Basis | If wrong |
|---|---|---|
| | | |
